# 版本更新说明

## v1.1.0 (当前版本)

### 新增功能

- 新增媒体 ID 桥接接口 `/api/media-id-bridge`，支持通过 IMDb ID、豆瓣 ID、片名和年份查询关联媒体 ID
- 新增 GitHub Actions 手动部署 Cloudflare Worker 流程，可在 Actions 页面手动触发部署
- 新增 `wrangler.ci.toml`，用于 GitHub Actions 部署时读取无敏感信息的 Wrangler 配置

### 功能优化

- 保持媒体 ID 桥接接口与现有 PT-Gen API 鉴权、响应格式和缓存机制一致
- 补充媒体 ID 桥接接口的 HTTP Client 示例
- 更新 README，增加媒体 ID 桥接接口和 GitHub Actions 手动部署说明

### 技术改进

- 将媒体 ID 桥接逻辑拆分为独立模块 `worker/src/utils/mediaIdBridge.js`
- 调整 Worker 路由，新增 `/api/media-id-bridge` 独立接口层级
- 将 GitHub Actions 部署流程改为 `workflow_dispatch` 手动触发

## v1.0.9

### Bug Fix

- 修复英文搜索功能：IMDb 搜索触发 AWS WAF 保护时自动回退到 TMDB
- 修复豆瓣搜索数据结构处理问题

### 功能优化

- 优化 IMDb 请求头配置，避免主动触发反爬机制
- 重构 README.md 文档，改进 API 接口描述结构和层级
- 整理 API 测试文件 `http/pt-gen.http`，按平台分类组织
- 统一版本维护机制：以 `package.json` 为单一版本来源，自动同步到各组件
- 前端页面 header 显示版本号（从 Vite 构建注入）

### 新增功能

- 添加版本同步脚本 `scripts/sync-version.mjs`
- 添加 GitHub Actions 自动同步 fork 工作流
- 图片代理接口支持更多平台（豆瓣、Steam 等）
- GitHub 构建流程集成版本同步步骤

### 文档更新

- README.md 接口文档重构，按平台分类、统一请求方式描述
- 添加版本维护说明和发布流程文档
- 更新 GitHub 项目链接

## v1.0.8

### 新增功能

- HMAC-SHA256 认证签名机制作为前后端验证机制
- 增加日志功能，方便开发测试

### 功能优化

- 更改豆瓣搜索为爬取方式，弃用之前的外部 API
- 优化 IMDb 的请求头避免出现挑战
- 重构目录结构和重命名部分文件名称
- 更改前端部分数据显示样式

### Bug Fix

- 修复前端 API 可修改请求头绕过验证，防止 API 被滥用

## v1.0.7

## v1.0.6

### 新增功能

- 增加了对豆瓣读书和QQ音乐的支持

### 功能优化

- 优化前端显示来源的站点显示
- 优化格式化输出函数

## v1.0.5

### 新增功能

-

增加了中文搜索支持豆瓣,该搜索API稳定性有待观察,所以如果出现搜索失败时会自动回退使用TMDB进行搜索,暂时知道这个豆瓣搜索有些关键词是会搜索失败的,例如"
拯救"、"地球"等

- 增加了后端API功能支持GET请求,但务必要携带key参数才可以正常使用
- 增加了支持获取红果短剧平台的信息功能,感谢[@jiongjiongJOJO](https://github.com/jiongjiongJOJO)贡献

### 功能优化

- 优化了Bangumi元数据的演员信息不再元数据里构建展示格式,交由格式函数输出时构建格式化
- 优化了豆瓣获取数据时演员信息超时导致无法获取的问题,增加了重试机制

### 组件更新

- 由于React在2025年11月29日爆出一个重大的安全漏洞,根据官方修复,已更新React版本至19.2.1及其相关依赖组件的更新版本

## v1.0.4

### 新增功能

- 添加了对OurBits静态数据缓存的支持，作为API调用失败时的备用数据源
- 实现了豆瓣奖项信息的结构化解析和展示
- 增加了对豆瓣、IMDb、Bangumi和Steam平台的缓存优化

### 功能优化

- 改进了数据抓取和处理逻辑，提高了稳定性和准确性
- 优化了错误处理机制，提供了更清晰的错误信息
- 增强了对反爬虫机制的应对能力

### 技术改进

- 重构了代码结构，提高了模块化程度和可维护性
- 更新了依赖库和工具链，提升了整体性能
- 完善了工具函数文档，方便开发者使用和二次开发
