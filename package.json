{
  "name": "pt-gen-refactor",
  "author": "Jerold",
  "version": "1.1.0",
  "description": "Cloudflare Worker for PT-Gen Refactor",
  "main": "worker/index.js",
  "scripts": {
    "sync-version": "node scripts/sync-version.mjs",
    "dev": "npm run sync-version && cd worker && npx wrangler dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run sync-version && cd frontend && npm run build",
    "deploy": "npm run build && cd worker && npx wrangler deploy",
    "release": "npm run sync-version && npm run build && npm run deploy"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "pt-gen"
  ],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.33.2"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
